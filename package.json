{
  "name": "invadrix",
  "version": "0.1.3",
  "description": "Classic Space Invaders clone — single-file HTML5 Canvas game with zero dependencies",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "dev": "echo 'dev'",
    "build": "echo 'No build step — single-file HTML5 game'",
    "test": "node --input-type=module < test.js"
  }
}
